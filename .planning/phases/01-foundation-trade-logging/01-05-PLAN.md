---
phase: 01-foundation-trade-logging
plan: 05
type: execute
wave: 3
depends_on: ["04"]
files_modified:
  - src/components/TradeForm.tsx
  - src/components/TradeList.tsx
  - src/app/layout.tsx
  - src/components/BottomNav.tsx
  - src/app/trades/page.tsx
  - src/components/ui/dialog.tsx
autonomous: true
requirements: [LOG-01, LOG-03, LOG-04, UI-01, UI-02]
user_setup: []
must_haves:
  truths:
    - "Trade entry uses modal dialog overlay, not inline form"
    - "TradeForm includes setup type dropdown, notes textarea, and screenshot URL/file input"
    - "TradeList displays all trade fields and filter/sort controls trigger page filter state"
    - "Export button triggers CSV/JSON download with current filter/date-range applied"
    - "Bottom tab navigation provides access to main sections (trades, analytics, import, settings)"
    - "Application layout is fully responsive and works on mobile viewport"
  artifacts:
    - path: "src/components/TradeForm.tsx"
      provides: "Modal-based trade creation form extended with setup, notes, screenshot fields"
      contains: "Dialog, Select for setupType, Textarea for notes"
    - path: "src/components/TradeList.tsx"
      provides: "Enhanced trade table with export dropdown, responsive overflow"
      contains: "Button with export menu"
    - path: "src/components/BottomNav.tsx"
      provides: "Fixed bottom tab bar with nav links"
      exports: ["BottomNav"]
    - path: "src/app/layout.tsx"
      provides: "Root layout that includes BottomNav above children"
      contains: "<BottomNav />"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/BottomNav.tsx"
      via: "import BottomNav and render before closing body"
      pattern: "import.*BottomNav"
    - from: "src/components/TradeForm.tsx"
      to: "src/app/api/trades/route.ts"
      via: "onSubmit calls fetch('/api/trades', { method: 'POST' })"
      pattern: "fetch\\('/api/trades'"
    - from: "src/components/TradeList.tsx"
      to: "src/app/api/trades/export/route.ts"
      via: "export button triggers window.location.href='/api/trades/export?format=csv'"
      pattern: "/api/trades/export"
---

<objective>
Polish the UI: convert inline form to modal, extend fields (setup/notes/screenshot), enhance trade list with export, add bottom tab navigation, and ensure responsive behavior across all screens.

Purpose: Phase 1 must deliver a production-quality, mobile-friendly interface. Modal pattern keeps context, extended fields meet LOG-03/04, export fulfills UI-02, and bottom nav provides D-17 navigation.

Output: Fully styled UI components that feel like a cohesive app.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trade-logging/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md

<interfaces>
From existing code:
- TradeForm currently: onSubmit receives any with symbol, direction, entryPrice, exitPrice, quantity.
- TradeList currently: expects { trades: any[] } and renders Table with columns: Symbol, Direction, Entry, Exit, P&L, Date, Setup.

Required new props/interfaces:
- Setup type field: shadcn Select with options from SetupType enum.
- Notes: textarea.
- Screenshot: file input that reads file and uploads to a temp storage (since backend screenshotUrl expects string URL). Simplification: store base64 data URL or skip in Phase 1? Decision: screenshotUrl is optional; Phase 1 may allow manual URL entry or omit. We can provide simple file input that reads as DataURL and stores as base64 string; but prisma field is String, can hold base64. That's acceptable.
- Export: button that toggles dropdown (CSV / JSON). For CSV, include date range from filter state (passed from parent). Could query params: ?format=csv&startDate=...&endDate=... The export endpoint already accepts query params (D-21). So TradeList should have access to current filters to construct URL. That can be passed as prop from trades page. The TradeList component can be extended with onFilterChange callback, or we compute export URL in parent.

We will adjust TradeList to receive an optional `filters` prop and generate export link accordingly.

For modal: use shadcn/ui Dialog. Install: `npx shadcn@latest add dialog`. Ensure controlled open state from parent; Trades page should manage dialog state and pass tradeCreate function.

Given this plan modifies existing components, read_first directive would include reading current component to understand props. In our task descriptions, we'll instruct to read the current file.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert TradeForm to modal and add extended fields</name>
  <files>src/components/TradeForm.tsx</files>
  <action>
  1. Read current src/components/TradeForm.tsx to understand props.
  2. Install shadcn Dialog if not present: `npx shadcn@latest add dialog -y`.
  3. Upgrade TradeForm to accept `open` and `onOpenChange` props and wrap form in Dialog component. Keep onSubmit behavior.
  4. Add new fields:
     - Setup Type: Select with options from SetupType enum.
     - Notes: Textarea with maxLength 1000.
     - Screenshot URL: simple Input; Phase 1 stores URL string (no upload).
  5. Ensure form validation (client-side) mirrors Zod schema basics.
  6. Update src/app/trades/page.tsx (modified):
     - Add state for dialog open.
     - Render a button to open modal.
     - Render Dialog with TradeForm inside, passing open,onOpenChange,onSubmit that calls API and closes on success.
     - Ensure after successful submission, trade list is refreshed (by re-fetch or state update).
  7. Imports for Dialog components from '@/components/ui/dialog'.
  </action>
  <verify>
    <automated>grep -q "Dialog" src/components/TradeForm.tsx && echo "Modal added" && grep -q "open" src/app/trades/page.tsx && echo "Trade page uses modal"</automated>
  </verify>
  <done>TradeForm is a modal dialog with additional fields (setup, notes, screenshot URL)</done>
</task>

<task type="auto">
  <name>Task 2: Enhance TradeList with export and responsive scroll</name>
  <files>src/components/TradeList.tsx</files>
  <action>
  1. Read current TradeList.tsx.
  2. Extend component props to accept optional `filters` object (symbol, direction, startDate, endDate) used to build export URL.
  3. Add an "Export" dropdown button above table (shadcn DropdownMenu) offering "Export CSV" and "Export JSON". On click, construct URL: `/api/trades/export?format=${fmt}` plus encode current filters as query params; trigger download by creating anchor element and clicking, or `window.location.href`.
  4. Wrap Table in a `div` with overflow-x-auto for horizontal scroll on small screens.
  5. Optionally add sorting indicators; not required.
  </action>
  <verify>
    <automated>grep -q "export" src/components/TradeList.tsx && grep -q "overflow-x-auto" src/components/TradeList.tsx && echo "Export UI and scroll added"</automated>
    <manual>On /trades, click Export → CSV downloads; table scrolls horizontally on mobile</manual>
  </verify>
  <done>TradeList now has export dropdown with CSV/JSON, and responsive horizontal scrolling</done>
</task>

<task type="auto">
  <name>Task 3: Add bottom tab navigation and global layout</name>
  <files>
    - src/components/BottomNav.tsx (new)
    - src/app/layout.tsx (modified)
  </files>
  <action>
  1. Install lucide-react icons if needed (already in deps).
  2. Create src/components/BottomNav.tsx:
     - Fixed bottom bar (fixed bottom-0 left-0 right-0) with flex row of 4 items: Trades, Analytics, Import, Settings.
     - Use shadcn Button or custom list with Link from next/navigation.
     - Icons: FileText (trades), BarChart2 (analytics), Upload (import), Settings.
     - Highlight active route based on pathname (use usePathname from next/navigation).
  3. Update src/app/layout.tsx:
     - Import BottomNav.
     - Wrap children in a div with padding-bottom equal to nav height (e.g., pb-16) to avoid content hidden behind fixed nav.
     - Render <BottomNav /> at end of body (after {children}).
  4. Ensure Tailwind classes for styling: bg-background border-t, etc.
  </action>
  <verify>
    <automated>test -f src/components/BottomNav.tsx && grep -q "fixed bottom" src/components/BottomNav.tsx && echo "BottomNav exists"</automated>
    <manual>On mobile viewport, bottom bar is fixed; on wide screens also present; links navigate correctly</manual>
  </verify>
  <done>Bottom navigation added across all pages; layout adapted to avoid overlap</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → UI components | User interactions that trigger API requests; components must enforce client-side checks but server is source of truth |
| Modal form → submission | Screenshot URL string accepted (client-provided); no validation of external URL yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-A | Tampering | Screenshot URL field (free text) | accept | No server-side validation of URL format beyond Zod string; arbitrary URL allowed but harmless |
| T-05-B | Information Disclosure | Trade data rendered in list | accept | Already protected by API per-user filtering |
| T-05-C | Denial of Service | Rapid modal submissions | accept | Rate limiting deferred to Phase 2 |

</threat_model>

<verification>
1. Trades page now opens modal on "Add Trade"; modal includes extra fields
2. New fields (setup, notes, screenshot) are sent and accepted by API
3. Export dropdown in trade list triggers file download (CSV/JSON) with current filter params
4. Bottom nav appears on all pages, active link highlights
5. Mobile responsiveness: nav usable, modal fits small screens, table scrolls horizontally
</verification>

<success_criteria>
- [ ] TradeForm renders inside Dialog with setup select, notes textarea, screenshot URL input
- [ ] TradeList includes Export dropdown and responsive horizontal scroll container
- [ ] BottomNav shows four icons/links; route highlighting works
- [ ] Layout applies padding so page content not obscured by fixed BottomNav
- [ ] All new UI elements follow existing shadcn/ui styling conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trade-logging/01-05-SUMMARY.md`
</output>

</plan>
