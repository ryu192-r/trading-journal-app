---
phase: 01-foundation-trade-logging
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - src/app/trades/page.tsx
  - src/app/analytics/page.tsx
  - src/app/import/page.tsx
  - package.json
autonomous: true
requirements: [LOG-01, LOG-02, ANLY-01, ANLY-02]
user_setup: []
must_haves:
  truths:
    - "Trades page displays trade list with filter/sort UI and create button"
    - "Analytics page shows P&L summary cards and equity curve chart"
    - "Import page provides CSV upload form and displays import results"
    - "All pages use responsive Tailwind layout and available on desktop/mobile"
  artifacts:
    - path: "src/app/trades/page.tsx"
      provides: "Main trades listing page with filters, trade table, and quick NL entry bar"
      contains: "export default function TradesPage"
    - path: "src/app/analytics/page.tsx"
      provides: "Analytics dashboard with win rate, profit factor, and equity curve charts"
      contains: "export default function AnalyticsPage"
    - path: "src/app/import/page.tsx"
      provides: "CSV import page with file picker, broker hint, and status display"
      contains: "export default function ImportPage"
  key_links:
    - from: "src/app/trades/page.tsx"
      to: "src/components/TradeList.tsx"
      via: "import TradeList and pass trades prop"
      pattern: "import.*TradeList"
    - from: "src/app/analytics/page.tsx"
      to: "src/app/api/analytics/pnl-summary/route.ts"
      via: "fetch('/api/analytics/pnl-summary')"
      pattern: "pnl-summary"
    - from: "src/app/import/page.tsx"
      to: "src/app/api/import/route.ts"
      via: "fetch('/api/import' with FormData)"
      pattern: "/api/import"
---

<objective>
Build user-facing pages for trades management, analytics viewing, and CSV import. Together they complete the core user workflow: view trades, see performance, and bring in bulk history.

Purpose: The backend is ready; we need accessible pages that traders will use daily. Responsive design ensures mobile usability (UI-01).

Output: Three route pages under /trades, /analytics, and /import with functional UI and fetch integration.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trade-logging/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md

<interfaces>
Existing components to reuse:
- src/components/TradeList.tsx: expects { trades: TradeOutput[] }
- src/components/TradeForm.tsx: expects onSubmit(data: any); currently minimal, will be upgraded later
- shadcn/ui components: Card, Button, Input, Select, Table

API endpoints to consume:
- GET /api/trades?symbol=...&sortBy=...&sortOrder=...
- GET /api/analytics/pnl-summary
- GET /api/analytics/equity-curve
- POST /api/import (multipart/form-data)
- POST /api/parse (for NL quick entry)

Chart library: Research recommends Chart.js 4.x. Already in package.json? Check: package.json doesn't include chart.js yet. We need to decide: Should we add chart.js here? Better to include dependency install as part of task (npm install chart.js). It's acceptable.

Responsive layout: use Tailwind grid/flex. D-17 requires bottom tab bar with 4 tabs (Trades, Analytics, Import, Settings). That will be added in Plan 05. These pages should render content above the nav; nav added globally later.

Date formatting: Use toLocaleDateString with Indian format? Not critical now.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trades listing page with filters and NL quick entry</name>
  <files>src/app/trades/page.tsx</files>
  <action>
  1. Create directory src/app/trades if needed.
  2. Install any needed shadcn components: `npx shadcn@latest add card input select dropdown-menu` (if not already).
  3. Write src/app/trades/page.tsx:
     - Fetch trades on mount: GET /api/trades (include query params for filters)
     - State: trades array, loading, filters (symbol, direction, startDate, endDate, sortBy, sortOrder)
     - UI: Top bar with filter inputs (symbol text, direction select, start/end date), sort control, and "Add Trade" button that opens modal (placeholder for now; modal implemented in Plan 05).
     - Add a "Quick add (NL)" section: text input and button that POSTs to /api/parse, on success refresh list.
     - Render TradeList component with trades, display loading indicator, empty state.
     - Use Tailwind responsive classes.
  4. Example hook for fetch: use client component with 'use client'.
  5. Populate effect to load trades whenever filters change.
  </action>
  <verify>
    <automated>test -f src/app/trades/page.tsx && grep -q "export default function TradesPage" src/app/trades/page.tsx && echo "Trades page file created"</automated>
    <manual>Navigate to /trades, ensure list loads, filters change request to /api/trades with query params</manual>
  </verify>
  <done>Trades page built with filter UI, NL quick entry bar, and TradeList integration</done>
</task>

<task type="auto">
  <name>Task 2: Create analytics dashboard page</name>
  <files>src/app/analytics/page.tsx</files>
  <action>
  1. Create src/app/analytics/page.tsx.
  2. Install Chart.js for equity curve visualization:
    ```bash
    npm install chart.js react-chartjs-2
    ```
  3. Page fetches both summary and curve endpoints on mount.
  4. Display summary cards: win rate, profit factor, avg win, avg loss, expectancy, total trades (from pnl-summary).
  5. Display equity curve line chart (using Chart.js Line) showing cumulative P&L over time with drawdown shading (data from equity-curve endpoint).
  6. Use Tailwind grid for responsive layout; cards on mobile stack, chart responsive.
  7. Handle loading/error states.
  </action>
  <verify>
    <automated>test -f src/app/analytics/page.tsx && grep -q "chart.js\|Chart" src/app/analytics/page.tsx && echo "Analytics page with chart lib imported"</automated>
    <manual>Open /analytics; verify summary numbers displayed and line chart renders</manual>
  </verify>
  <done>Analytics page built with summary metrics and equity curve visualization</done>
</task>

<task type="auto">
  <name>Task 3: Create CSV import page</name>
  <files>src/app/import/page.tsx</files>
  <action>
  1. Create src/app/import/page.tsx.
  2. UI: File input accepting .csv, submit button; on submit POST to /api/import with FormData.
  3. Display upload status: uploading spinner; on response show success/failure counts and detailed error rows (if any).
  4. Include short instructions: "Upload broker tradebook CSV (Zerodha, Dhan, AngelOne, ICICI Direct)".
  5. Use shadcn Card, Button, Progress (optional) components.
  6. Ensure the page is responsive.
  </action>
  <verify>
    <automated>test -f src/app/import/page.tsx && grep -q "formData" src/app/import/page.tsx && echo "Import page created"</automated>
    <manual>Visit /import, upload a test CSV, verify success message and new trades appear in list</manual>
  </verify>
  <done>Import page built with CSV upload, feedback UI, and error display</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /trades, /analytics, /import pages | UI interactions that trigger authenticated API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-A | Information Disclosure | Analytics data displayed | accept | Auth ensures user isolation; no cross-user leakage — already handled in API |
| T-04-B | Tampering | Client-side filter parameters | mitigate | API re-applies userId scoping; server-side filtering enforced |
| T-04-C | Denial of Service | Client-side rapid page refreshes | accept | General rate limiting deferred to Phase 2 |

</threat_model>

<verification>
1. GET /trades returns page with filter bar, trade list, and NL quick entry
2. GET /analytics shows cards and line chart (Chart.js canvas present)
3. GET /import renders file picker and status area
4. All pages render correctly on mobile (viewport < 768px) and desktop
5. Navigation between pages works via browser routing (React Link components not yet added; direct URL works)
</verification>

<success_criteria>
- [ ] Trades page loads trades by default; filters trigger API calls with query params
- [ ] NL quick entry on /trades successfully creates trade and refreshes list
- [ ] Analytics page displays all 5 summary metrics and equity curve chart
- [ ] Import page allows CSV upload and shows result summary and any row errors
- [ ] All pages are responsive (Tailwind classes work on narrow screens)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-trade-logging/01-04-SUMMARY.md`
</output>

</plan>
