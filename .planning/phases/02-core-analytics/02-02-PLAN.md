---
phase: 02-core-analytics
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/api/analytics/r-multiples/route.ts
  - src/app/api/analytics/performance/route.ts
  - src/app/api/analytics/mae/route.ts
  - src/app/api/analytics/mfe/route.ts
  - src/app/api/analytics/setup-scorecard/route.ts
  - src/app/api/analytics/market-regime/route.ts
  - src/app/api/analytics/weekly-chart/route.ts
  - src/app/api/trades/route.ts
  - src/app/api/import/route.ts
  - src/app/api/parse/route.ts
autonomous: true
requirements: [ANLY-04, ANLY-05, MAE-02, SETUP-01, SETUP-02, MKT-02, MKT-03]
user_setup: []
must_haves:
  truths:
    - "GET /api/analytics/r-multiples returns histogram bins (-2R to +5R) and distribution statistics (mean, median, skew)"
    - "GET /api/analytics/performance returns Sharpe, Sortino, max drawdown, recovery factor"
    - "GET /api/analytics/mae returns MAE histogram and stop optimization recommendation"
    - "GET /api/analytics/mfe returns MFE statistics including early exit detection"
    - "GET /api/analytics/setup-scorecard returns per-setup metrics and allocation recommendations"
    - "GET /api/analytics/market-regime returns regime breakdown and adaptive rules"
    - "GET /api/analytics/weekly-chart returns weekly Nifty candles with trade markers"
    - "All trade creation endpoints (POST /api/trades, POST /api/import, POST /api/parse) automatically set trade.regime based on entry date"
  artifacts:
    - path: "src/app/api/analytics/r-multiples/route.ts"
      provides: "R-multiple histogram endpoint"
      exports: ["GET"]
    - path: "src/app/api/analytics/performance/route.ts"
      provides: "Performance metrics endpoint (Sharpe, Sortino, max drawdown, recovery factor)"
      exports: ["GET"]
    - path: "src/app/api/analytics/mae/route.ts"
      provides: "MAE histogram and stop optimization recommendation endpoint"
      exports: ["GET"]
    - path: "src/app/api/analytics/mfe/route.ts"
      provides: "MFE statistics endpoint"
      exports: ["GET"]
    - path: "src/app/api/analytics/setup-scorecard/route.ts"
      provides: "Setup performance scorecard with allocation suggestions"
      exports: ["GET"]
    - path: "src/app/api/analytics/market-regime/route.ts"
      provides: "Performance by market regime and adaptive rule text"
      exports: ["GET"]
    - path: "src/app/api/analytics/weekly-chart/route.ts"
      provides: "Weekly chart data for Nifty/Sensex with trade overlays"
      exports: ["GET"]
  key_links:
    - from: "any analytics route"
      to: "src/lib/analytics/calculator"
      via: "import { computeRMultiple, computeSharpe, ... }"
      pattern: "import.*analytics/calculator"
    - from: "weekly-chart/route.ts"
      to: "dataFetcher.getDailyOHLC"
      via: "fetch ^NSEI weekly candles"
      pattern: "getDailyOHLC"
    - from: "trades/route.ts"
      to: "classifyMarketRegime"
      via: "sets trade.regime on creation"
      pattern: "classifyMarketRegime"
---

<objective>
Expose analytics via RESTful API endpoints: R-multiple distribution, performance ratios, MAE/MFE with stop optimization, setup scorecard, market regime breakdown, and weekly Nifty chart overlay.

Purpose: These endpoints power the frontend visualizations and provide the core analytics capabilities required by Phase 2 success criteria. They encapsulate the calculator logic and enforce authentication and per-user data scoping.

Output: Seven protected GET endpoints serving JSON data to the UI.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md

<interfaces>
Existing auth & DB integration:
- requireAuth() from '@/middleware/auth' returns { userId: string }
- prisma from '@/lib/db' queries trades table

New calculator exports (from Plan 01):
```typescript
export function computeRMultiple(pnl: number, entryPrice: number, stopPrice: number, direction: Direction): number;
export function computeSharpe(returns: number[], riskFreeRate?: number): number;
export function computeSortino(returns: number[], target?: number): number;
export function computeMaxDrawdown(equity: number[]): number;
export function computeRecoveryFactor(totalProfit: number, maxDrawdown: number): number;
export function computeMAE(trade: any, priceData?: {low: number}[]): {maeAbs: number, maeR: number} | null;
export function computeMFE(trade: any, priceData?: {high: number}[]): {mfeAbs: number, mfeR: number} | null;
export function classifyMarketRegime(entryDate: Date): Promise<string>;
export function computeSetupScorecard(trades: any[]): Array<{setupType: string; winRate: number; avgR: number; profitFactor: number; bestTrade: number; worstTrade: number; recommendation: string}>;
export function computeGhostTrade(trade: any, postExitData: any): any;
export function computeWeeklyOverlay(symbol: string, weekStart: Date): { candles: Array<{date: Date, open: number, high: number, low: number, close: number}>, tradeMarkers: Array<{date: Date, type: 'entry'|'exit', direction: string, price: number}> };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement R-multiples histogram and performance metrics endpoints</name>
  <files>src/app/api/analytics/r-multiples/route.ts, src/app/api/analytics/performance/route.ts</files>
  <action>
  1. Create directory structure `src/app/api/analytics/r-multiples` and `.../performance` with `route.ts` files.
  2. For r-multiples:
     - Import requireAuth, prisma, computeRMultiple.
     - Query trades with exitPrice and stopPrice for user.
     - Compute R for each via computeRMultiple; filter nulls.
     - Create histogram bins from -2R to +5R step 0.5.
     - Compute mean, median, skewness of R distribution.
     - Return JSON: { histogram: [...], stats: {count, mean, median, skew} }.
  3. For performance:
     - Import computeSharpe, computeSortino, computeMaxDrawdown, computeRecoveryFactor.
     - Derive R-values from trades (same as above) and build cumulative R equity curve.
     - Compute metrics on returns and equity.
     - Return JSON: { sharpe, sortino, maxDrawdown, recoveryFactor, tradesCount }.
  4. Ensure all endpoints use requireAuth and filter by userId.
</action>
  <verify>
    <automated>test -d src/app/api/analytics/r-multiples && test -d src/app/api/analytics/performance && grep -q "computeRMultiple" src/app/api/analytics/r-multiples/route.ts && grep -q "computeSharpe" src/app/api/analytics/performance/route.ts && echo "R-multiples & performance endpoints created"</automated>
  </verify>
  <done>R-multiples distribution and performance metrics endpoints implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement MAE and MFE endpoints</name>
  <files>src/app/api/analytics/mae/route.ts, src/app/api/analytics/mfe/route.ts</files>
  <action>
  1. Create route files:
  2. In mae/route.ts:
     - Require auth.
     - Fetch trades with exitPrice and stopPrice.
     - For each trade, call computeMAE(trade). Collect maeAbs, maeR values.
     - Build histogram of maeR (bins from -2R to 0 step 0.2R).
     - For winners (pnl>0), compute loss percentage = maeAbs / entryPrice, sort and compute 95th percentile; craft recommendation string.
     - Return { histogram, sampleSize, winnersCount, stopRecommendation }.
  3. In mfe/route.ts:
     - Require auth.
     - Fetch trades with exitPrice and targetPrice.
     - For each, computeMFE(trade). Collect mfeR and hitTarget flag (mfeAbs >= (targetPrice-entryPrice)).
     - Compute averageMFE and targetHitRate.
     - Return { tradesCount, averageMFE, targetHitRate }.
  3. Ensure computeMAE/ MFE can fetch price data if not provided: They should call getDailyOHLC internally based on trade dates.
</action>
  <verify>
    <automated>test -f src/app/api/analytics/mae/route.ts && grep -q "computeMAE" src/app/api/analytics/mae/route.ts && test -f src/app/api/analytics/mfe/route.ts && grep -q "computeMFE" src/app/api/analytics/mfe/route.ts && echo "MAE/MFE endpoints exist"</automated>
  </verify>
  <done>MAE and MFE endpoints created with histogram and recommendation logic</done>
</task>

<task type="auto">
  <name>Task 3: Add regime logging to trade creation and build setup/regime/weekly endpoints</name>
  <files>
    - src/app/api/trades/route.ts
    - src/app/api/import/route.ts
    - src/app/api/parse/route.ts
    - src/app/api/analytics/setup-scorecard/route.ts
    - src/app/api/analytics/market-regime/route.ts
    - src/app/api/analytics/weekly-chart/route.ts
  </files>
  <action>
  This plan modifies existing creation endpoints and adds three new analytics endpoints.

  **Part A – Regime logging on creation:**
  - Edit `src/app/api/trades/route.ts` POST handler: after building the trade data object but before prisma.trade.create, add `regime: await classifyMarketRegime(entryDate)`. (entryDate default to new Date()).
  - Edit `src/app/api/import/route.ts`: within the transaction loop where validated trade data is prepared, compute regime using entryDate and add to object.
  - Edit `src/app/api/parse/route.ts`: Same as trades; compute regime and add.
  - Note: ensure `classifyMarketRegime` is imported from `@/lib/analytics/calculator`.

  **Part B – New analytics endpoints:**

  1. `src/app/api/analytics/setup-scorecard/route.ts`:
     - Fetch user's closed trades (exitPrice not null) with setupType not null.
     - Group by setupType.
     - For each group: count, wins, losses, avgR (mean computeRMultiple), profitFactor = sum(gains)/sum(abs(losses)), best/worst R.
     - Allocation recommendation: simple heuristic: if avgR > 0, "Consider increasing weight"; if avgR < 0, "Avoid or reduce"; neutral: "Maintain current allocation".
     - Return JSON array of objects.

  2. `src/app/api/analytics/market-regime/route.ts`:
     - Get closed trades; for each compute regime using classifyMarketRegime(entryDate). Cache?

     - Aggregate per regime: count, wins, losses, avgR, profitFactor.
     - Generate rule: if avgR < 0 or winRate < 40% => "Avoid trading in {regime}"; else "Can trade {regime} — edge positive".
     - Return array.

  3. `src/app/api/analytics/weekly-chart/route.ts`:
     - Optional query: week (YYYY-WW). Default to current week Monday.
     - Fetch ^NSEI daily candles for the week via getDailyOHLC.
     - Fetch user's trades with entryDate in the week.
     - Build markers array: each trade yields entry marker, and if exitDate also within week, exit marker.
     - Return { candles, tradeMarkers }.

  All endpoints use requireAuth and filter by user.
</action>
  <verify>
    <automated>for f in src/app/api/trades/route.ts src/app/api/import/route.ts src/app/api/parse/route.ts; do grep -q "classifyMarketRegime" $f && echo "$f updated"; done && test -d src/app/api/analytics/setup-scorecard && test -d src/app/api/analytics/market-regime && test -d src/app/api/analytics/weekly-chart && echo "New endpoints created"</automated>
  </verify>
  <done>Regime field set on all trades; setup, regime, weekly endpoints operational</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API → requireAuth middleware | Every endpoint must be authenticated |
| API → Calculator | Data trusted from DB; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | Spoofing | JWT tampering | mitigate | requireAuth validates signature against JWT_SECRET |
| T-2-05 | Tampering | Query parameter manipulation | mitigate | Server-side filters always include userId extracted from verified token |
| T-2-06 | Information Disclosure | Cross-user analytics leakage | mitigate | Each query uses `userId` from token; verified |
</threat_model>

<verification>
1. Regime field is present after creating a trade via POST /api/trades or /api/parse
2. GET /api/analytics/r-multiples returns histogram and stats; performance endpoint returns numeric ratios
3. GET /api/analytics/mae returns histogram, stopRecommendation; MFE returns hitRate
4. GET /api/analytics/setup-scorecard returns per-setup aggregation including allocation recommendation
5. GET /api/analytics/market-regime returns data for all six regime types with adaptive rule text
6. GET /api/analytics/weekly-chart returns candles and markers for current week
7. All endpoints return 401 when called without auth cookie
</verification>

<success_criteria>
- [ ] R-multiples endpoint provides histogram (bins -2R to +5R) and distribution stats
- [ ] Performance endpoint computes Sharpe, Sortino, max drawdown, recovery factor
- [ ] MAE endpoint includes histogram and stop recommendation from winner MAE percentiles
- [ ] MFE endpoint reports average MFE and target hit rate for trades with targets
- [ ] Setup scorecard lists each setup with win rate, avg R, profit factor, best/worst, allocation tip
- [ ] Market regime breakdown includes all six regimes with performance metrics and adaptive rule guidance
- [ ] Weekly chart endpoint delivers daily candles and trade entry/exit markers for overlay rendering
- [ ] All trade creation flows (manual, CSV, NL) store regime automatically
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-analytics/02-02-SUMMARY.md`
</output>

</plan>
