---
phase: 02-core-analytics
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/analytics/RMultipleHistogram.tsx
  - src/app/analytics/r-multiple/page.tsx
  - src/components/analytics/MAEHistogram.tsx
  - src/app/analytics/mae/page.tsx
  - src/components/analytics/SetupScorecardTable.tsx
  - src/components/analytics/MarketRegimeBreakdown.tsx
  - src/components/analytics/WeeklyOverlayChart.tsx
  - src/app/analytics/advanced/page.tsx
autonomous: true
requirements: [ANLY-04, ANLY-05, MAE-02, SETUP-01, SETUP-02, MKT-02, MKT-03]
user_setup: []
must_haves:
  truths:
    - "User can navigate to /analytics/r-multiple and see a histogram of R outcomes with bin counts and stats"
    - "User can navigate to /analytics/mae and see MAE histogram and stop optimization recommendation"
    - "User can navigate to /analytics/advanced and view Setup Scorecard table, Market Regime analysis, and Weekly Nifty overlay chart"
    - "All charts are responsive and use Chart.js with appropriate types (bar, line)"
  artifacts:
    - path: "src/components/analytics/RMultipleHistogram.tsx"
      provides: "Bar chart component for R-multiple distribution"
      contains: "export default function RMultipleHistogram"
      min_lines: 30
    - path: "src/app/analytics/r-multiple/page.tsx"
      provides: "Page that fetches R-multiples endpoint and renders histogram + stats"
      contains: "export default function RMultiplesPage"
    - path: "src/components/analytics/MAEHistogram.tsx"
      provides: "Bar chart for MAE distribution and recommendation card"
      contains: "export default function MAEHistogram"
    - path: "src/app/analytics/mae/page.tsx"
      provides: "MAE analysis page fetching /api/analytics/mae"
      contains: "export default function MAEPage"
    - path: "src/components/analytics/SetupScorecardTable.tsx"
      provides: "Table component for setup performance metrics"
      contains: "export default function SetupScorecardTable"
    - path: "src/components/analytics/MarketRegimeBreakdown.tsx"
      provides: "Cards/table for regime breakdown and adaptive rules"
      contains: "export default function MarketRegimeBreakdown"
    - path: "src/components/analytics/WeeklyOverlayChart.tsx"
      provides: "Line chart with Nifty weekly prices and trade markers"
      contains: "export default function WeeklyOverlayChart"
    - path: "src/app/analytics/advanced/page.tsx"
      provides: "Container page that fetches three endpoints and renders three components"
      contains: "export default function AdvancedAnalyticsPage"
  key_links:
    - from: "src/app/analytics/r-multiple/page.tsx"
      to: "src/app/api/analytics/r-multiples/route.ts"
      via: "fetch('/api/analytics/r-multiples')"
      pattern: "fetch\\(['\"]/api/analytics/r-multiples['\"]"
    - from: "src/app/analytics/mae/page.tsx"
      to: "src/app/api/analytics/mae/route.ts"
      via: "fetch('/api/analytics/mae')"
      pattern: "fetch\\(['\"]/api/analytics/mae['\"]"
    - from: "src/app/analytics/advanced/page.tsx"
      to: "/api/analytics/setup-scorecard"
      via: "fetch('/api/analytics/setup-scorecard')"
      pattern: "setup-scorecard"
---
<objective>
Build the frontend analytics pages and components: R-multiples histogram with stats, MAE histogram with stop recommendation, and an advanced section displaying Setup Scorecard, Market Regime breakdown, and Weekly Nifty overlay chart.

Purpose: These pages deliver the visual insights users need. They consume the API endpoints from Plan 02 and render them with Chart.js, completing the analytics value chain.

Output: Three route pages (`/analytics/r-multiple`, `/analytics/mae`, `/analytics/advanced`) with reusable chart components.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md

<interfaces>
Charts use Chart.js + react-chartjs-2 already present. Responsive layout with shadcn/ui cards.  
API response contracts (expected shapes):
- r-multiples: { histogram: [{label, count}], stats: {count, mean, median, skew} }
- performance: { sharpe, sortino, maxDrawdown, recoveryFactor, tradesCount }
- mae: { histogram: [...], stopRecommendation: string }
- mfe: { tradesCount, averageMFE, targetHitRate }
- setup-scorecard: { setups: [{ setupType, winRate, avgR, profitFactor, bestTrade, worstTrade, recommendation }] }
- market-regime: { regimes: [{ regime, trades, winRate, avgR, profitFactor, recommendation }] }
- weekly-chart: { candles: [{date, open, high, low, close}], tradeMarkers: [{date, type, direction, price}] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create R-multiple histogram component and page</name>
  <files>src/components/analytics/RMultipleHistogram.tsx, src/app/analytics/r-multiple/page.tsx</files>
  <action>
  1. Create `src/components/analytics/RMultipleHistogram.tsx` using `react-chartjs-2` Bar:
     - Accept `bins` prop.
     - Prepare data: labels = bins.map(b=>b.label), data = bins.map(b=>b.count).
     - Render Bar inside a Card; include axis labels.
  2. Create `src/app/analytics/r-multiple/page.tsx` ('use client'):
     - Fetch `/api/analytics/r-multiples` on mount.
     - Render cards for stats (mean, median, skew) above histogram.
     - Render RMultipleHistogram component.
     - Add loading and error states.
  3. Use Tailwind responsive container classes.
</action>
  <verify>
    <automated>test -f src/components/analytics/RMultipleHistogram.tsx && grep -q "Bar" src/components/analytics/RMultipleHistogram.tsx && test -f src/app/analytics/r-multiple/page.tsx && grep -q "RMultipleHistogram" src/app/analytics/r-multiple/page.tsx && echo "R-multiples page ready"</automated>
  </verify>
  <done>R-multiple histogram component and page implemented</done>
</task>

<task type="auto">
  <name>Task 2: Create MAE histogram page with stop recommendation</name>
  <files>src/components/analytics/MAEHistogram.tsx, src/app/analytics/mae/page.tsx</files>
  <action>
  1. Create `src/components/analytics/MAEHistogram.tsx`:
     - Bar chart from `bins` prop (similar to RMultipleHistogram).
     - Below chart, render a Card containing the stopRecommendation string (large text).
  2. Create `src/app/analytics/mae/page.tsx` ('use client'):
     - Fetch `/api/analytics/mae`.
     - Display MAEHistogram with data.
     - Optionally display `winnersCount` and `sampleSize` as metrics.
  3. Add basic layout with Card containers.
</action>
  <verify>
    <automated>test -f src/components/analytics/MAEHistogram.tsx && grep -q "Bar" src/components/analytics/MAEHistogram.tsx && test -f src/app/analytics/mae/page.tsx && echo "MAE page created"</automated>
  </verify>
  <done>MAE histogram component with recommendation and page created</done>
</task>

<task type="auto">
  <name>Task 3: Create Advanced Analytics page (Setup, Regime, Weekly)</name>
  <files>
    - src/components/analytics/SetupScorecardTable.tsx
    - src/components/analytics/MarketRegimeBreakdown.tsx
    - src/components/analytics/WeeklyOverlayChart.tsx
    - src/app/analytics/advanced/page.tsx
  </files>
  <action>
  1. Create `src/components/analytics/SetupScorecardTable.tsx`:
     - Table with columns: Setup, Win Rate, Avg R, Profit Factor, Best, Worst, Recommendation.
     - Accept `setups` prop.
  2. Create `src/components/analytics/MarketRegimeBreakdown.tsx`:
     - Show each regime as a Card or table row with metrics and recommendation text.
     - Could include a simple bar chart for avg R across regimes (optional).
  3. Create `src/components/analytics/WeeklyOverlayChart.tsx`:
     - Fetch from `/api/analytics/weekly-chart` (default week).
     - Use Chart.js Line for close prices.
     - Overlay markers: use PointElement for entries (green up arrows for long, red down for short) and exits (different shape).
  4. Create `src/app/analytics/advanced/page.tsx`:
     - Use Promise.all to fetch all three endpoints.
     - Render three sections stacked: SetupScorecardTable, MarketRegimeBreakdown, WeeklyOverlayChart.
     - Use Cards for sections.
  5. Implement loading/error states.
</action>
  <verify>
    <automated>test -f src/components/analytics/SetupScorecardTable.tsx && test -f src/components/analytics/MarketRegimeBreakdown.tsx && test -f src/components/analytics/WeeklyOverlayChart.tsx && test -f src/app/analytics/advanced/page.tsx && echo "Advanced analytics page and components created"</automated>
  </verify>
  <done>Advanced analytics page with Setup, Regime, and Weekly components completed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client pages → Analytics APIs | Components call endpoints; rely on cookies for auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | Analytics data in browser | accept | Auth already enforced at API; client receives only own data |
| T-3-02 | Tampering | Client-side fetch URL manipulation | mitigate | API validates userId server-side; tampered URLs still scoped |
</threat_model>

<verification>
1. Navigate to /analytics/r-multiple and confirm chart renders and stats show numbers
2. Navigate to /analytics/mae and confirm histogram bars appear; stop recommendation text visible
3. Navigate to /analytics/advanced and confirm all three sections load with data (table, regime list, line chart with markers)
4. All pages responsive in mobile view (≤ 768px)
5. Network tab shows successful fetches to respective endpoints (200)
</verification>

<success_criteria>
- [ ] R-multiple page displays histogram and descriptive statistics
- [ ] MAE page displays histogram and actionable stop recommendation
- [ ] Setup Scorecard table shows per-setup win rate, avg R, profit factor, best/worst, allocation tip
- [ ] Market Regime section shows six-region breakdown with rule suggestions
- [ ] Weekly Overlay displays Nifty week line chart with entry/exit dots, color-coded by direction
- [ ] All pages are fully responsive and styled consistently with shadcn/UI
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-analytics/02-03-SUMMARY.md`
</output>

</plan>
